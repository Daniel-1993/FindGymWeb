<%= form_for(modalidade) do |f| %>
  <% if modalidade.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(modalidade.errors.count, "error") %> prohibited this modalidade from being saved:</h2>

      <ul>
      <% modalidade.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :descricao %>
    <%= f.text_field :descricao %>
  </div>

  <div class="field">
    <%= f.label :treinador_id %>
    <%= f.select :treinador_id, options_for_select(Treinador.all{|m|[m.nomeProfessor, m.id]},f.object.treinador_id)%>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
